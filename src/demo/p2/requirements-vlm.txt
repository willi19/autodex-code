# Robot-PC runtime for src/demo/p2/semantic_router.py. The official Qwen
# checkpoint is loaded in bitsandbytes NF4 (4-bit) mode at runtime. This is
# compatible with the installed RTX 3090 CUDA stack, unlike the matching AWQ
# checkpoint's lm_head Triton kernel.
transformers==4.51.3
accelerate>=1.2,<2
qwen-vl-utils==0.0.8
bitsandbytes>=0.48.2,<0.49
